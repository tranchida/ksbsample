apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ksbsample-ingress
spec:
  rules:
  - host: tranchida-a.dynv6.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ksbsample-service
            port:
              number: 8080
